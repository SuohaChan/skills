# Skill 编写与演进指南

这份文档是本仓库的 Skill 作者规范，供未来的 Agent 和维护者在新增、修改或评估 Skill 前阅读。

目标不是让 Skill 变得更长，而是让它在不同输入、不同上下文和不同时间下保持稳定、可解释、可维护。

## 核心原则

把 Skill 当成经过测试的工作流代码，而不是不断增长的个人提示词。

一次对话只能产生候选经验，不能直接成为通用规则。只有经过多个不同案例验证、并且明确了边界，规则才适合进入正式 Skill。

Skill 的职责是规定任务中的行为策略；工具、接口、配置和运行环境仍以代码、MCP schema、配置文件和命令帮助为事实来源。

## 写作前先判断是否值得做成 Skill

优先把以下任务沉淀为 Skill：

- 会重复执行，并且有相对稳定的处理顺序；
- 需要持续遵守边界、失败处理或安全约束；
- 输出有稳定结构或可检查的质量标准；
- 仅靠一次性对话容易遗漏步骤或产生返工。

以下内容通常不应单独做成 Skill：

- 一次性的个人偏好；
- 只适用于一个输入文件的特殊处理；
- 可以直接从配置、代码或 `--help` 中读取的事实；
- 没有稳定触发条件、只能靠当前对话上下文理解的临时要求。

## 从对话提炼规则

先记录原始对话中的事实，再提炼规则。至少区分：

1. 用户真正想完成的任务；
2. Agent 执行过的步骤；
3. 用户纠正过的错误；
4. 哪些纠正是普遍适用的，哪些只是这次案例的偏好；
5. 任务成功的可观察标准；
6. 不应触发或不应继续执行的相邻情况。

把具体案例改写成可迁移的判断。例如，不要写“把华东区域放在第二部分”，而应写“沿用用户提供的业务分组；没有明确分组时，按任务类型或时间顺序组织”。

如果无法把规则写成适用于至少三种不同输入的行为，先把它留在候选记录中，不要写入正式 Skill。

## Skill 的信息层级

保持渐进式披露：

```text
frontmatter
  -> 只说明做什么、何时触发
SKILL.md
  -> 核心步骤、边界、完成条件
references/
  -> 只有特定分支需要的规则和背景
scripts/
  -> 确定性、重复性或容易出错的机械操作
assets/
  -> 模板、字体、示例资源
```

`SKILL.md` 应让 Agent 能够完成主流程，不应成为实现代码、百科全书或所有历史讨论的汇总。

把只属于某个分支的资料放进 `references/`。把稳定、机械、可由程序准确完成的步骤放进 `scripts/`。环境已经提供的事实不要在 Skill 中复制，除非 Agent 无法通过查找获得它，或者这个事实背后的原因和陷阱本身就是工作流的一部分。

每个步骤都要有完成条件。完成条件应能回答“这一步什么时候算做完”，并且尽量可以被文件、工具结果、字段或输出结构检查。

## Frontmatter 和触发描述

`description` 同时承担能力说明和触发路由两个职责，应明确：

- Skill 能完成什么；
- 哪些用户意图或任务分支应该触发；
- 哪些相邻请求不应触发。

触发描述应覆盖不同的自然说法，但不要把同一分支堆成关键词列表。描述越宽，越需要补充“不触发”的边界和负例。

不要因为某个测试案例使用了特定产品名、文件名或句式，就把它写进 description。description 描述任务类别，而不是测试样本。

## 防止过拟合的评测流程

每个有明确行为的 Skill 至少准备四类测试：

### 触发测试

验证哪些请求应该使用 Skill，哪些近邻请求不应该使用 Skill。

建议至少包含：

- 8–10 个应该触发的真实口语请求；
- 8–10 个不应该触发的相邻请求；
- 同一任务的不同说法、输入形态和上下文完整度。

### 质量测试

验证 Skill 触发后是否完成了任务，而不是只检查是否读过 `SKILL.md`。

### Holdout 测试

保留一组开发时不用于改规则的输入。它只用于最后验证 Skill 是否泛化到没有见过的表达和案例。

### 回归测试

把历史失败案例固定下来。后续修改不能解决新问题却重新引入旧问题。

评测时优先比较：

```text
无 Skill
旧 Skill
新 Skill
```

至少观察：触发准确性、任务完成度、遗漏、误触发、额外步骤、工具调用顺序、运行成本和安全边界。

如果新规则只改善了原始案例，却让 holdout 或负例变差，应判断为过拟合，回退或重新抽象规则。

## 证据和变更记录

每个成熟 Skill 应记录：

- 规则来源：来自哪些真实任务或需求；
- 适用范围：它解决哪一类问题；
- 非目标：它明确不解决什么；
- 已知限制：哪些情况仍需人工判断；
- 测试覆盖：已有正例、反例、holdout 和回归案例；
- 变更原因：为什么加入、删除或改写某条规则。

建议在 Skill 目录中使用 `CHANGELOG.md` 或在 eval 文件中保留来源说明。不要把完整对话原文塞进 `SKILL.md`；原始对话应保存在独立的工作记录或候选经验目录中。

## 修改现有 Skill 的顺序

未来 Agent 修改 Skill 时，按以下顺序执行：

1. 先阅读本指南、仓库的 `SKILL-DEVELOPMENT.md` 和目标 Skill 的全部文件；
2. 说明当前 Skill 的目标、触发边界、已知失败和测试覆盖；
3. 先设计或补充测试，再提出规则修改；
4. 区分通用规则、个人偏好、当前实现细节和外部事实；
5. 优先删除过时、重复或环境中已有的内容，再增加新规则；
6. 修改后运行结构检查、脚本测试和 Skill 行为评测；
7. 检查正例、负例、holdout 和历史回归案例；
8. 在变更记录中说明证据、影响和未覆盖部分；
9. 只有测试结果支持泛化时，才合并到正式 Skill。

在没有完成第 2 步和第 3 步之前，不要直接重写 `SKILL.md`。

## 对本仓库的特别约定

- `D:\project\skill` 是 Skill 的唯一事实来源；运行目录使用 SymbolicLink 或 Junction。
- 不要把本机缓存、截图、生成物、真实凭据或真实服务配置提交进仓库。
- 不要因为一次调用成功就把当前路径、当前数据源或当前输出样式写死为通用规则。
- MCP 提供能力，Skill 规定使用时机和流程；不要在 Skill 中复制 MCP 的实现。
- 需要危险或不可逆操作时，必须写明显式授权、歧义处理、重试策略、失败含义和敏感信息边界。
- 主 Skill 保持短而可执行；细节应按分支放入 references，机械操作应由脚本承担。

## 参考资料

- Anthropic Agent Skills：<https://github.com/anthropics/skills>
- Agent Skills 开放规范：<https://github.com/Open-Dot-Agents/SKILL.md>
- OpenAI Skills 与 skill-creator：<https://github.com/openai/skills>
- agnix Agent 配置检查器：<https://github.com/agent-sh/agnix>
- agent-skill-eval：<https://github.com/tardigrde/agent-skill-eval>

