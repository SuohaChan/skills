# obsidian-rag 评测说明

`evals.json` 是训练、回归和人工评审案例；`holdout.json` 是独立集合，设计或修改 Skill 时不要根据它的单个结果硬编码规则。

评测关注四类行为：

1. 是否在依赖个人记录时触发本地检索；
2. 是否在普通知识或用户已提供完整内容时避免不必要检索；
3. 是否在摘要不足、无结果或来源冲突时正确处理证据；
4. 是否给出可回到原始笔记的引用，而不是编造路径、标题或行号。

当前仓库没有独立的多代理行为评测运行器。不能实际执行 Agent 行为对照时，应记录为待人工验证，不得填写虚构的通过率。
